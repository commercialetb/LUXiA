services:
  - type: web
    name: luxia-engine
    env: python
    plan: free
    region: frankfurt
    buildCommand: pip install -r apps/engine/requirements.txt
    startCommand: uvicorn apps.engine.engine_api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
